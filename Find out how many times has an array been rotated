class Solution {
    public int findKRotation(List<Integer> arr) {
        // Code here
        int ans =0;
        for(int i=0;i<arr.size()-1;i++){
            if(arr.get(i)>arr.get(i+1)){
                ans=i+1;
                break;
            }
        }
        return ans;
    }
}
