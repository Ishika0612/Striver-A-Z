class Solution {
    public int findMaxConsecutiveOnes(int[] nums) {
       int ones=0;
       int maxOnes=0;
       for(int i=0;i<nums.length;i++){
        if(nums[i]==1)
        ones++;
        else
        ones=0;
        maxOnes=Math.max(ones,maxOnes);
       } 
       return maxOnes;
    }
}
